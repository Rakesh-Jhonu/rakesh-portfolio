import {useEffect,useRef} from "react";
import * as THREE from "three";
const Scene=()=>{const ref=useRef<HTMLDivElement|null>(null);useEffect(()=>{if(!ref.current)return;const el=ref.current;const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(18,el.clientWidth/el.clientHeight,.1,100);camera.position.set(0,0,17.5);const renderer=new THREE.WebGLRenderer({alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(el.clientWidth,el.clientHeight);renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.2;el.appendChild(renderer.domElement);const geo=new THREE.IcosahedronGeometry(0.82,5);const mat=new THREE.MeshPhysicalMaterial({color:"#b992df",metalness:.35,roughness:.2,clearcoat:.65,clearcoatRoughness:.2});const mesh=new THREE.Mesh(geo,mat);scene.add(mesh);scene.add(new THREE.AmbientLight(0xffffff,1.3));const key=new THREE.DirectionalLight(0xffffff,4);key.position.set(4,6,8);scene.add(key);const rim=new THREE.PointLight(0xc88cff,9,20);rim.position.set(-4,2,5);scene.add(rim);let raf=0;const clock=new THREE.Clock();const animate=()=>{const t=clock.getElapsedTime();mesh.rotation.y=t*.16;mesh.rotation.x=Math.sin(t*.45)*.06;mesh.position.y=Math.sin(t*.8)*.12;renderer.render(scene,camera);raf=requestAnimationFrame(animate)};animate();const resize=()=>{if(!el.clientWidth)return;camera.aspect=el.clientWidth/el.clientHeight;camera.updateProjectionMatrix();renderer.setSize(el.clientWidth,el.clientHeight)};window.addEventListener("resize",resize);return()=>{cancelAnimationFrame(raf);window.removeEventListener("resize",resize);geo.dispose();mat.dispose();renderer.dispose();if(el.contains(renderer.domElement))el.removeChild(renderer.domElement)}},[]);return <div className="character-container"><div className="character-model" ref={ref}><div className="character-rim"/></div></div>};
export default Scene;
